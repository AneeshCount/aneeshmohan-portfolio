name: CI

# main is covered by Deploy, which runs the same verify workflow before it
# publishes. Running it here too would just duplicate the job on every push.
on:
  push:
    branches-ignore: [main, gh-pages]
  pull_request:

jobs:
  verify:
    uses: ./.github/workflows/verify.yml
